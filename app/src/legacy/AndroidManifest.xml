<?xml version="1.0" encoding="utf-8"?>
<!--
  Extra permissions needed only on Android 11 and below.

  Before API 31 there were no split Bluetooth permissions: scanning for a device
  required the legacy BLUETOOTH/BLUETOOTH_ADMIN pair *and* the location
  permission, because a Bluetooth scan can reveal position. From API 31 the
  BLUETOOTH_SCAN permission with neverForLocation replaces all of that, which is
  why the modern flavour asks for no location access at all.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!--
      ACCESS_FINE_LOCATION above makes aapt imply android.hardware.location as a
      REQUIRED feature, and the package manager then refuses to install on any
      device without a location provider. That is exactly how this app became
      uninstallable on a rugged barcode terminal, reporting only a generic
      "Can't install the app". Declaring them optional is mandatory here.
    -->
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.location.network" android:required="false" />
</manifest>
